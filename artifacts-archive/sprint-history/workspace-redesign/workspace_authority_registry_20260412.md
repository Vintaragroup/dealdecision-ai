# Workspace Authority Registry — April 12, 2026

Legend:
- **Consumer** = exact React component/hook using the field.
- **Backend source** = canonical data structure expected to populate the UI.
- **Allowed fallback order** = descending priority; only move to the next step when the previous one is unavailable.
- **Forbidden fallback** = legacy paths that must be blocked to keep deterministic truth alignment.
- **Trust state** = structured data readiness required before displaying (e.g., `structured_summary.ready`, `render_package.status_summary`).

| UI field / label | Consumer (file:line) | Backend source of truth | Allowed fallback order | Forbidden fallback path(s) | Trust state requirement |
|---|---|---|---|---|---|
| Company name (hero header) | `DealWorkspace_overviewTab_v3` via `WorkspaceOverviewVM.companyName` (`apps/web/src/components/workspace/builders/buildWorkspaceViewModel.ts:569-609`) | `report.structured_summary.company_name` populated by compiler-simple RC-S6 (`packages/core/src/reports/compiler-simple.ts:2811-2815`) | 1) `structured_summary.company_name` 2) governed overlay `overview.claims.company` (once evidence-backed) 3) Deal metadata `dealInfo.name` (placeholder only if 1–2 missing) | Raw `dealData.name` or user-entered profile name overriding structured extraction permanently | `GET /api/v1/deals/:id/report` ready + compiler version ≥38 |
| Company description (hero paragraph) | Same as above (`buildWorkspaceViewModel`) and rendered in `DealOverviewTab` (`apps/web/src/components/workspace/DealWorkspace_overviewTab_v3.tsx:116-131`) | `structured_summary.deal_summary_v1.one_liner` / `tiers.hero` | 1) `deal_summary_v1` hero when `ready=true` 2) deterministic `structuredDealSummaryLong` 3) governed overlay hero when deterministic missing 4) placeholder copy | Phase1 `overviewV2.product_solution` sentences, overlay fallback preceding deterministic copy | `structured_summary.deal_summary_v1.ready === true` |
| Product summary card | `DealOverviewTab` (prop `productSummary`) -> `workspaceOverviewModel.keyFacts.product` (`apps/web/src/components/pages/DealWorkspace.tsx:3964-3994`) | `report.structured_summary.product_summary_v1.value` | 1) `product_summary_v1.value` 2) `deal_summary_v1.product.text` 3) deterministic overview slots (`selectDeterministicOverviewSlotsV1`) 4) governed overlay facts 5) Phase1 DIO | Direct use of `overviewV2.product_solution` or `executive_summary_v*.product_solution` before deterministic values | `structured_summary.ready` + `report.deal_summary_v1.ready` |
| Market / ICP card | Same path as product (`apps/web/src/components/pages/DealWorkspace.tsx:3945-3979`) | `structured_summary.market_summary_v1.value` | 1) `market_summary_v1.value` 2) `deal_summary_v1.market_target.text` 3) deterministic overview slots 4) overlay facts 5) Phase1 DIO | Using `overviewV2.market_icp` ahead of structured text | `structured_summary.ready` |
| Business model card | `workspaceOverviewModel.keyFacts.business_model` feeding `DealOverviewTab` and top header (`apps/web/src/components/pages/DealWorkspace.tsx:3945-3994`) | `structured_summary.kpis.business_model` (with evidence) or arbitration result (`selectAuthoritativeBusinessModelV1`) | 1) Arbitration (`phase1.business_model_arbitration_v1`) 2) `structured_summary.kpis.business_model` 3) `structured_summary.business_model_summary` (once evidence-backed) 4) legacy Phase1 fallback (temporary) | Overlay/governed copy overriding arbitrated value; DIO `business_model` chosen before structured KPIs when ready | Arbitration ready OR `report.structured_summary` ready |
| Raise tile / card | `DealOverviewTab` `snapshotFacts.raise` + `workspaceOverviewModel.keyFacts.raise_terms` (`apps/web/src/components/pages/DealWorkspace.tsx:3945-3994`) | `structured_summary.raise` (`reportCanonicalRaise`) | 1) `structured_summary.raise.value` 2) `deal_summary_v1.raise` 3) deterministic slot raise 4) overlay raise (LLM) 5) Phase1 DIO text | Using `reportView.raise`/overlay before structured summary when ready, or nulling to “—” while DIO text exists | `structured_summary.ready` |
| Funding stage label | `DealWorkspaceHeader` via `selectDealWorkspaceHeader` (`apps/web/src/components/workspace/DealWorkspaceTopSection.tsx:103-206`) | `report.funding_stage_v1` signal | 1) `funding_stage_v1.funding_stage` 2) profile override (if user-specified) | Hard-coded “Pre-Seed/Idea” scoreboard default | `report.ready` |
| KPI tiles (Revenue, Growth, Customers) | Same header component (`apps/web/src/components/pages/DealWorkspace.tsx:3708-3759`) | `report.structured_summary.kpis` | 1) `selectedHeader.ready ? structured kpis : null` 2) deterministic fallback (only when structured missing) | Regex-derived `topSectionRevenue/topSectionGrowth` text mining; Phase1 values | `structured_summary.ready` + coverage policy allow flags |
| Investment snapshot paragraph | `DealOverviewTab` (prop `investmentSnapshotBody`) from `investment_analysis_overview_v2.summary_medium` (`apps/web/src/components/pages/DealWorkspace.tsx:5405-5419`) | `governed_overview.overview_json.investment_analysis_overview_v2.summary_medium` | 1) deterministic `investment_analysis_overview_v2.summary_medium` (report) 2) overlay summary 3) fallback to concatenated product/market/business model paragraphs | DIO hero paragraphs overriding canonical medium summary | `investor_insights.report.status in {ready, deterministic_only}` |
| Deal Deep Dive sections (market/product/etc.) | `DealDeepDiveTab` (`apps/web/src/components/workspace/DealDeepDiveTab.tsx:1-410`) | `/api/v1/deals/:id/deep-dive` response built via `buildDealDeepDiveV1` (`packages/core/src/reports/deep-dive-v1.ts`) | Single source: deterministic deep dive payload; no fallback | Injecting overlay summaries or Phase1 nodes into the deep-dive UI | Deep-dive route returns `deep_dive` object |
| Investor Insights hero score & modules | `InvestorInsightsTab` and `InsightsDataPanel` (`apps/web/src/components/workspace/InvestorInsightsTab.tsx`, `InsightsDataPanel.tsx`) | `investor_insight_reports.render_package` + `status_summary` | 1) render_package sections 2) fallback placeholder text when section missing | Legacy `AIDealAssistant` mock content; phase1 nodes | `status_summary.report_status in {ready, deterministic_only}` |
| Financial Audit tiles (burn/runway, metrics) | `FinancialAuditTab` via `useFinancialAuditData` (`apps/web/src/components/workspace/financial-audit/FinancialAuditTab.tsx`, `apps/web/src/hooks/useFinancialAuditData.ts`) | `financial_breakdown_v1`, `financial_integrity_v1`, `financial_coverage_v1`, `underwriting_readiness_v1` | 1) authoritative selectors 2) placeholder banners when selectors null | Pulling burn/runway from regex or top-section heuristics | `selectAuthoritativeFinancial*` available (i.e., `/report` ready) |
| Red flags list | Evidence accordion inside overview tab (`apps/web/src/components/pages/DealWorkspace.tsx:8450-8486`) | `report.redFlags` + promoted fact pipeline | 1) `report.redFlags` 2) `promoted_facts.red_flag_v1` 3) placeholder “No red flags” | Overlay warnings or LLM hallucinations overriding deterministic red flags | Report ready |
| Evidence counters | Same panel as red flags (`apps/web/src/components/pages/DealWorkspace.tsx:8420-8449`) | `reportView.evidenceIds` derived from structured summary sources | Single deterministic source | Recomputing from Phase1 nodes | Structured summary ready |
| Deal pipeline status / diligence phase | Header badges (`apps/web/src/components/workspace/builders/buildWorkspaceViewModel.ts:520-565`) | `dealInfo.pipeline_status` + job stream | 1) SSE/computed status 2) fallback “Not Started” | Hard-coded placeholder | Job stream available |

## Notes
- Compiler-simple RC-S6 enrichments (company name, team highlights, fund deployment, UOF, project pipeline, revenue model) are already present in `structured_summary`. Any UI consumer should reference those keys directly instead of DIO text.
- `selectDealWorkspaceOverviewModel` (`apps/web/src/lib/selectors/selectDealWorkspaceOverviewModel.ts`) is the enforcement point for deterministic→overlay fallbacks. Any new field must follow the same pattern to stay deterministic.
