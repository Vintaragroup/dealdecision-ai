# DealDecision AI — Changelog

## 2025-12-16
- Added Fastify jobs status endpoints and analyze trigger with Redis + Postgres wiring.
- Worker now runs BullMQ processors for ingest, evidence fetch, and deal analysis with progress updates.
- Web refreshed with theme overrides and updated Deal Workspace UI; live-mode guards now call the new API client.

