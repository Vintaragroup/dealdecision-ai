# Deal Workspace UI Slot Map

Note: the canonical version of this doc now lives at `apps/web/src/lib/docs/deal-workspace-ui-slot-map.md`.

Internal developer note: this document maps stable UI placement anchors (`data-slot=...`) to their owning components and canonical report inputs.

| Slot ID | Component | File path | Data selector/prop | Canonical report path | Guard rule | Notes |
|---|---|---|---|---|---|---|
| `header.score.subsummary` | `DealWorkspaceTopSection` | `apps/web/src/components/workspace/DealWorkspaceTopSection.tsx` | Prop: `dealSummaryShort` → DOM: `data-slot="header.score.subsummary"` | `report.structured_summary.deal_summary_v1.one_liner` | Deterministic-only; no overlay replacement. If missing, render nothing. | Short deterministic deal summary rendered directly below the score ring. |
| `header.tiles.burn` | `DealWorkspaceTopSection` | `apps/web/src/components/workspace/DealWorkspaceTopSection.tsx` | Prop: `burn` → DOM: `data-slot="header.tiles.burn"` | Guard: `report.financial_coverage_v1.coverage.burn_rate_present` (aka “burn_present”); Value: `report.structured_summary.kpis.burn_rate` | Only display a burn value when coverage says present. Even when present, render `—` if deterministic numeric burn is missing. Never fall back to overlay/legacy for a value. | Tooltip is sourced from deterministic `report.financial_coverage_v1.evidence.burn_rate_present.snippet` when available. |
| `header.tiles.runway` | `DealWorkspaceTopSection` | `apps/web/src/components/workspace/DealWorkspaceTopSection.tsx` | Prop: `runway` → DOM: `data-slot="header.tiles.runway"` | Guard: `report.financial_coverage_v1.coverage.runway_present`; Value: `report.structured_summary.kpis.runway_months` | Only display a runway value when coverage says present. Even when present, render `—` if deterministic numeric runway is missing. Never fall back to overlay/legacy for a value. | Tooltip is sourced from deterministic `report.financial_coverage_v1.evidence.runway_present.snippet` when available. |
| `topSummary.dealSummary.long` | `DealWorkspaceTopSection` | `apps/web/src/components/workspace/DealWorkspaceTopSection.tsx` | Prop: `dealSummary` → DOM: `data-slot="topSummary.dealSummary.long"` | Primary: `report.structured_summary.deal_summary_v1.long_summary` (fallback: `report.deal_summary_v1.tiers.hero/overview/deep`) | Prefer deterministic structured summary; do not use overlay to replace deterministic blocks. | `dealSummary` is passed from `DealWorkspace` and rendered as paragraphs (split on blank lines). |
| `topSummary.dealSummary.short` | (not present) | (not present) | N/A | Candidate: `report.deal_summary_v1.one_liner.text` | N/A | There is currently no distinct “short” Deal Summary region in the top summary component; the closest equivalent lives in the overview card one-liner (not the top summary). |
| `keyFacts.product` | `DealWorkspaceOverviewComp` | `apps/web/src/components/workspace/dealworkspace_overview_comp.tsx` | DOM: `data-slot="keyFacts.product"` + `data-testid="key-fact-product"`; Prop: `product` | Primary: `report.structured_summary.product_summary_v1.value` (fallbacks exist) | Prefer deterministic structured summary when ready; fall back to deterministic report summary or Phase 1 when canonical is unavailable. | Value selection is performed in `DealWorkspace` before passing into the overview component. |
| `keyFacts.market` | `DealWorkspaceOverviewComp` | `apps/web/src/components/workspace/dealworkspace_overview_comp.tsx` | DOM: `data-slot="keyFacts.market"` + `data-testid="key-fact-market"`; Prop: `marketIcp` | Primary: `report.structured_summary.market_summary_v1.value` (fallbacks exist) | Prefer deterministic structured summary when ready; fall back to deterministic report summary or Phase 1 when canonical is unavailable. | Value selection is performed in `DealWorkspace` before passing into the overview component. |
