# Workspace Field Contract Test Plan (2026-04-12)

Each investor-facing slot must ship with an automated contract test that verifies: (a) the authoritative source populates the UI, (b) only the approved fallback order executes, (c) forbidden fallbacks are blocked, and (d) the section only renders once the required trust state is satisfied.

| Field / UI label | Authoritative source | Allowed fallback order | Forbidden fallback(s) | Trust state | Test assertions |
|---|---|---|---|---|---|
| Company name (Identity strip) | `report.structured_summary.company_name` (`packages/core/src/reports/compiler-simple.ts`) | 1. structured name 2. overlay company claim (once evidence-backed) 3. placeholder “Unnamed Deal” | Direct reads from `dealInfo.name` / `dealData.name` once structured name present | `/api/v1/deals/:id/report.ready === true` | Snapshot `DealWorkspace` with mocked report showing company name → expect header text matches structured field and local metadata ignored. When field missing, expect placeholder + “Not extracted” badge. |
| Deal type label | `report.metadata.deal_type` | 1. structured `deal_type` 2. fallback to intake `dealInfo.type` | Phase1 heuristics | Report ready | Assert `deal_type` text equals structured value; toggling to null triggers fallback label + warning badge. |
| Stage badge | `report.funding_stage_v1.funding_stage` | 1. funding_stage_v1 2. manual profile override 3. placeholder | Hard-coded “Pre-Seed/Idea” default; phase1 stage label | Report ready | Test `selectDealWorkspaceHeader` returns stage === structured stage; drop to override when stage absent. |
| Raise amount | `report.structured_summary.raise.value` + `round_label` | 1. structured summary 2. `deal_summary_v1.raise` 3. deterministic slot raise 4. display “Not disclosed” | `reportView.raise` DIO text; overlay copy preceding structured stage | Structured summary ready & canonical raise present | Render hero tile with `report.raise` set, verify text and evidence tooltip. When `null_rule` triggered, expect “Not disclosed” text and no DIO string. |
| Product summary card | `report.structured_summary.product_summary_v1.value` | 1. product_summary_v1 2. `deal_summary_v1.product` 3. deterministic overview slot 4. overlay fact 5. placeholder “Not extracted” | Phase1 `overviewV2.product_solution` preempting structured values | Structured summary ready | Mount `selectDealWorkspaceOverviewModel` with mock inputs; assert chosen field equals canonical order and that DIO path never executes when `product_summary_v1` exists. |
| Market / ICP card | `report.structured_summary.market_summary_v1.value` | 1. market_summary_v1 2. `deal_summary_v1.market_target` 3. deterministic slot 4. overlay fact 5. placeholder | Phase1 `overviewV2.market_icp` | Structured summary ready | Similar test verifying fallback order and that overlay only used when deterministic null. |
| Business model card | `selectAuthoritativeBusinessModelV1` (arbitration > structured KPI) | 1. arbitration 2. `structured_summary.kpis.business_model` 3. `business_model_summary` 4. legacy | Overlay/governed text overriding arbitration; DIO `business_model` used before structured KPI | Report ready or arbitration ready | Unit test `selectAuthoritativeBusinessModelV1` to expect arbitration path, structured path, and ensure `reportReadyButMissingStructuredSummary` fails closed. |
| Team highlights panel | `report.structured_summary.team_highlights[]` | 1. structured list 2. overlay highlight (if evidence-backed) 3. placeholder | Manual doc scraping; Phase1 slides | Report ready + compiler version ≥38 | Add tests ensuring `team_highlights` array renders pill list when present and shows “Not extracted” when empty. |
| Use of Funds breakdown | `report.structured_summary.use_of_funds_breakdown[]` | 1. structured breakdown 2. placeholder | DIO fallback; overlay text | Report ready + `null_rule` not triggered | Test aggregator ensures amounts sum and labels match; fallback yields “No use of funds extracted.” |
| Project pipeline | `report.structured_summary.project_pipeline[]` | 1. structured table 2. placeholder | DIO text; PDF screenshot images | Report ready + compiler version ≥38 | Component test verifying rows map to pipeline data, includes evidence link. |
| Revenue model | `report.structured_summary.revenue_model` | 1. structured revenue_model object 2. placeholder | Phase1 `business_model` text | Report ready | Snapshot ensures label, recurring flag, unit economics text from structured field. |
| Investment snapshot paragraph | `report.investment_analysis_overview_v2.summary_medium` | 1. deterministic summary 2. overlay summary 3. placeholder | Phase1 summary paragraphs | investor_insights.render_package ready | Validate toggling deterministic/overlay view works; overlay only used when deterministic missing. |
| Financial metrics (Revenue, ARR, Burn, Runway) | `selectAuthoritativeFinancialBreakdownV1`, `selectAuthoritativeBurnV1`, `selectAuthoritativeRunwayV1` | Single deterministic source, fallback displays “Not extracted” | Regex heuristics | `/report` ready + coverage allow flag true | Contract tests covering each selector’s output and tile rendering. |
| Integrity & coverage badges | `financial_integrity_v1`, `financial_coverage_v1` | 1. structured integrities 2. placeholder text | Manual risk flags | `/report` ready | Test severity mapping and tooltip content. |
| Underwriting readiness | `underwriting_readiness_v1` | Single source | Overlay inference | `/report` ready | Assert gauge value matches readiness status. |
| Red flags list | `report.redFlags` (merged promoted flags) | 1. structured red flags 2. placeholder “No red flags” | Overlay warnings | `/report` ready | Tests verifying message + severity sorting. |
| Open questions & implementation actions | Deep dive payload (`/deep-dive`) | Single source; fallback message | Overlay Q&A | Deep-dive ready | Tests confirming sections hide when payload missing, show prioritized items when present. |
| Investor Insights diagnostics | `investor_insights.render_package.sections` | Single source; fallback message referencing status summary | Mock data tables | Investor Insights report ready/deterministic_only | Tests verifying gating text when `status_summary.report_status !== ready` and that sections map to render_package keys. |
| Evidence carousel | `selectDeterministicOverviewSlotsV1` | 1. deterministic slots 2. placeholder | DIO segments | Render package ready | Assert each slot displays evidence IDs, clicking opens Evidence panel stub.
