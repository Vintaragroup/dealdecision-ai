# Local Docker Compose runtime environment
#
# Copy to `.env.docker.local` (kept untracked) and then recreate containers.
#
# Deterministic score bridge (v1)
# - `true` enables deterministic scoring application (still subject to drift gate)
# - `false` keeps baseline scoring
DETERMINISTIC_SCORE_V1_ENABLED=false
