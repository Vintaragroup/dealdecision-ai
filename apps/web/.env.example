# API base URL the web app calls at runtime.
# Local dev (docker-compose.dev.yml): http://localhost:9001
# Prod (Render): https://<your-api-service>.onrender.com
VITE_API_BASE_URL=http://localhost:9001
